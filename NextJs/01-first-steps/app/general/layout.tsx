export default function AboutLayout({
    children
}: {children: React.ReactNode}){
    return (
        <div>
            <h1>Hello Roout Layout about</h1>
            {children}
        </div>
    )
}
